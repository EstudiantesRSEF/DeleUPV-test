Para visualizar con el formato adecuado la página de manera local, hay que abrir una terminal dentro de la carpeta `/out` y ejecutar `npx serve .`. Abrir el enlace del output para visualizar.

_Nota: para ejecutar `npx` hay que tener instalado `Node.js` y `npm`._
